{
  "name": "ovb",
  "author": "Alan Shortis",
  "version": "1.0.0",
  "description": "ØVB is a cycling club.",
  "scripts": {
    "build": "ELEVENTY_ENV=production eleventy && npm run uglify",
    "dev": "eleventy --serve --quiet",
    "clean": "rm -rf dist",
    "uglify": "uglifycss dist/css/style.css > dist/css/style.min.css"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "uglifycss": "^0.0.29"
  }
}
